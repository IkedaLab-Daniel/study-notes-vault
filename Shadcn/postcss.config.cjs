module.exports = {
  plugins: {
    // Tailwind v4 uses a separate PostCSS plugin package
    // Install with: npm install -D @tailwindcss/postcss
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
